<%= simple_form_for([bank, bank.bank_balances.new], wrapper: :daisyui) do |f| %>

  <div class="flex gap-3 mb-4">
    <button type="button" class="btn btn-circle btn-primary btn-soft">
      <%= icon("dollar") %>
    </button>
    <div class="flex-1">
      <h3 class="text-lg font-bold">Crear Saldo Bancario</h3>
      <small class="label">Registra un nuevo saldo disponible para este banco</small>
    </div>
  </div>

  <%= f.input :amount,
          as: :string,
          input_html: {
            data: {
              controller: "number-input",
            },
          },
          hint: "Ingresa el monto disponible en #{bank.currency}" %>

  <%= f.input :rate,
          as: :string,
          input_html: {
            data: {
              controller: "number-input",
            },
          },
          hint: "Ejemplo: 1 CLP = X #{bank.currency}" %>

  <%= f.input :description,
          as: :text,
          input_html: {
            data: {
              controller: "currency-input",
            },
            class: "textarea textarea-bordered w-full",
            rows: 3,
            placeholder: "Ingresa una descripción para este saldo...",
          } %>

  <div class="modal-action">
    <form method="dialog">
      <button class="btn">Cancelar</button>
    </form>
    <%= f.submit "Crear Saldo", class: "btn btn-primary" %>
  </div>
<% end %>
