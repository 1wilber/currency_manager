<%= form_with(url: url, method: :get, html: {class: "search-form"}) do |f| %>
  <%= f.search_field :q,
                 class: "mb-0",
                 value: params[:q],
                 placeholder: t(".search", default: t("search")) %>
<% end %>
